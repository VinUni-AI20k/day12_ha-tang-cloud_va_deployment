version: '3.8'

services:
  agent:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - PORT=8000
      - AGENT_API_KEY=${AGENT_API_KEY:-h2a-secret-key}
    restart: always

  # Bạn có thể thêm redis vào đây nếu muốn làm stateless
  # redis:
  #   image: redis:alpine
